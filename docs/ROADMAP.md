# Roadmap

## Next milestones

1. App package signing and notarization docs for maintainers
2. Optional external CLI binary (`quicktype capture --target --text`)
3. richer stale bookmark remediation wizard UX
4. Optional markdown split preview in capture window
5. Full UI automation test suite on GitHub Actions
